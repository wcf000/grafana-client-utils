apiVersion: 1

datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  url: ${PROMETHEUS_SERVICE_URL}
  isDefault: true
- name: Tempo
  type: tempo
  access: proxy
  url: ${TEMPO_SERVICE_URL}
  isDefault: true

